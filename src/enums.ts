export enum GameMode {
	"daily",
	"hourly",
	"infinite",
	"minutely",
	"joke",
};

export const enum ms {
	SECOND = 1000 * 1,
	MINUTE = 1000 * 60,
	HOUR = 1000 * 60 * 60,
	DAY = 1000 * 60 * 60 * 24,
	JOKE = 0 * 0
};
